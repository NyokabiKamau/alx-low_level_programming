#include "main.h"

/**
 * main - prints the alphabet, lower case, by a new line
 *
 * Return: Always 0 (sucess)
 */
int main(void)
{
	char c;

	for (c = 'a'; 'a' <= 'z'; a++)
		_putchar('c');
	_putchar('\n');

	return (0);
}
